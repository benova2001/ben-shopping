specVersion: "v1"
serviceId: "shoppingapp"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "shoppingapp-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
